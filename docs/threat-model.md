# Threat Model

Assets include data, credentials, model inputs and outputs, tool permissions, provenance, and availability. Threats include injection, secret leakage, unauthorized actions, dependency compromise, poisoning, and denial of service. Controls: validation, least privilege, isolated adapters, secret hygiene, audit evidence, dependency auditing, and deterministic tests.
